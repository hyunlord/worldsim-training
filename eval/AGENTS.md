# Evaluation Directory Rules

- Store holdout inputs separately from evaluation reports.
- Keep automatic metrics and manual review notes in `reports/`.
- Reuse prepared datasets and artifact manifests instead of duplicating source data.
