# Prompt Asset Rules

- Keep prompt assets as reusable text files only.
- Avoid embedding execution logic or path assumptions here.
- Teacher prompts must stay aligned with the schema in `config/`.
- Validation prompts should describe review criteria, not implementation steps.
