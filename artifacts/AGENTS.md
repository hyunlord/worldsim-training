# Artifacts Directory Rules

- Store exported model outputs and traceable manifests only.
- `merged/` and `gguf/` are generated binary outputs and should stay out of git.
- `manifests/` should describe source datasets, settings, and export metadata.
