# Test Rules

- Prefer focused `pytest` coverage for pipeline contracts and regression checks.
- Use temporary directories instead of repo-local mutable fixtures when possible.
- Test behavior and file contracts, not private implementation details.
